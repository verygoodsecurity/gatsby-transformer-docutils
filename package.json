{
  "name": "gatsby-transformer-docutils",
  "description": "Gatsby plugin for parsing Docutils files.",
  "version": "1.0.0",
  "author": "very good security devs",
  "dependencies": {
    "@babel/runtime": "^7.8.7",
    "bluebird": "^3.7.1",
    "libxmljs": "^0.19.7",
    "lodash": "^4.17.15",
    "slash": "^3.0.0",
    "string.prototype.matchall": "^4.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.7.4",
    "@babel/core": "^7.7.4",
    "babel-preset-gatsby-package": "^0.2.12",
    "cross-env": "^5.2.1",
    "jest": "^24.9.0"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin"
  ],
  "main": "index.js",
  "peerDependencies": {
    "gatsby": "^2.0.15"
  },
  "scripts": {
    "build": "babel src --out-dir . --ignore **/__tests__",
    "prepare": "cross-env NODE_ENV=production npm run build",
    "watch": "babel -w src --out-dir . --ignore **/__tests__"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
